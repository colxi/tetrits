export class DOM {
  static infoBox = document.getElementById('infoBox') as HTMLElement
  static boardCanvas = document.getElementById('boardCanvas') as HTMLCanvasElement
  static UICanvas = document.getElementById('UICanvas') as HTMLCanvasElement
}
